const HomePage = () => {
    /*
    TODO:
    - Upload JSON (optional: file) submissions.
    - Start the AI Judge evaluation run
    - Show some overall high-level stats or summary of all queues/submissions (Optional)
     */

    return (
        <h1>Home Page</h1>
    );
}

export default HomePage;